{
  "name": "photoshare-tools",
  "private": true,
  "description": "Test harness for the photoshare app. The app itself ships no dependencies.",
  "scripts": {
    "test": "node tools/qr-verify.mjs && node tools/exif-test.mjs && node tools/e2e.mjs",
    "serve": "node tools/serve.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
